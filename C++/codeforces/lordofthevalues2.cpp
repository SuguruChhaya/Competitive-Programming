#include <bits/stdc++.h>
using namespace std;

int main(){
    long long t;
    cin >> t;
    for (long long i=0;i<t;i++){
        long long n;
        cin >> n;
        vector <long long> arr;
        for (long long j=0;j<n;j++){
            long long temp;
            cin >> temp;
            arr.push_back(temp);
        }
    }
}