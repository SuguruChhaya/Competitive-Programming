#include <bits/stdc++.h>

using namespace std;

int main(){
    //*I think this relates to the common factor stuff.
    //*Either we can increase the smallest or adjust one of the weird one that is causing trouble or reduce the largest. 
    //*But im not gonna do this one...
}