#include <bits/stdc++.h>
using namespace std;

int main(){
    long long x, n;
    cin >> x >> n;
    long long max = 0, minute = 0;
    for (long long i=0;i<n;i++){
        long long t,f;
        cin >> t >> f;
        //*I think it should be t-x<=0
        
    }
}