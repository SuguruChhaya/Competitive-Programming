#include <bits/stdc++.h>
using namespace std;

void solve(){
    //*How am I gonna do this without a brute force??
    //!Kind of picture a sliding window 
    //!One can be a sandwich but at least one has to be continuous. 
    //?Do I have to precalculate stuff?
    
}

int main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    long long t; cin >> t;
    while (t--){
        solve();
    }
}