#include <bits/stdc++.h>
using namespace std;

int main(){
    //*Hmm, how am I supposed to tell which direction?
    //*Can I look at the direction from which direction I came back and left the starting point?
    //*Even if an irregular shape, if I cover a fence, there must be a rectangular region that is gonna be covered.

    //!Hardcode direction detection. 
    long long n;cin >> n;
    for (long long i=0;i<n;i++){
        string s;
        cin >> s;
        //*Need to form a 2D vector and make sure it doesn't go out of bounds. 
        
        for (long long i=0;i<)
    }
}