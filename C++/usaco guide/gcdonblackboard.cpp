#include <bits/stdc++.h>
using namespace std;

int main(){
    //*Really mathy problem.
    //!How am I supposed to know??
    long long n;
    cin >> n;
    //*The key observation is that changing is not the focus. 
    //*Basically I can make a one that is holding back not hold back. 
    //*Which is basically eliminating. 
    //*So try eliminating all and see what the biggest is. 
    
}