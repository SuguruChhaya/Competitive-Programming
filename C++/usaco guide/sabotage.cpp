#include <bits/stdc++.h>
using namespace std;

int main(){
    //freopen("socdist.in", "r", stdin);
    //freopen("socdist.out", "w", stdout);
    long long n;
    cin >> n;
    vector <long long> arr (n);
    for (long long i=0;i<n;i++){
        cin >> arr[i];
    }
    //*Want to minimize the AVERAGE milk production of the remaining machines. 
}