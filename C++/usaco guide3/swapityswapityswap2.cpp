#include <bits/stdc++.h>
using namespace std;

int main(){
    long long n, m, k;
    cin >> n >> m >> k;
    vector<long long> l(m+1), r(m+1);
    vector<long long> mapping (n+1);
    for (long long i=1;i<=n;i++){
        mapping[i] = i;
    }
    for (long long i=0;i<m;i++){
        cin >> l[0]
    }
    //https://replit.com/@SuguruChhaya1/swap#main.cpp
}   