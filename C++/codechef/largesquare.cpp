#include <bits/stdc++.h>
using namespace std;

void solve(){
    long long n, a;
    cin >> n >> a;
    //*I think I basically have to check for the closest perfect square so it can divide into. 
    //https://stackoverflow.com/questions/28385544/algorithm-to-find-the-largest-square-number-smaller-than-n
    //*But no time to read this lol. 
}

int main(){
    long long t;
    cin >> t;
    while (t--){
        solve();
    }
}