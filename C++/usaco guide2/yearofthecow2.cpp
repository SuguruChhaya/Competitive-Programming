//!Must spend all 12 years in that cycle. 
#include <bits/stdc++.h>
using namespace std;

int main(){
    long long n, k;
    cin >> n >> k;
    for (long long i=0;i<n;i++){
        //?Why is it [x+11] / 12?
    }
}