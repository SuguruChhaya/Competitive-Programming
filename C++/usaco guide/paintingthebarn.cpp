#include <bits/stdc++.h>
using namespace std;

int main(){
    long long n, k;
    cin >> n >> k;
    //*What I need to be careful is that in the vector we are going top to bottom. 
    //!Also, difference between x-cord and ycoord compared to row and col. 
    //*Also if I make sure I change coordinate into blocks. 
    //*Basically the haystacking problem but in 2D which makes it harder. 
    //*Try later again. 
    for (long long i=0;i<n;i++)
}