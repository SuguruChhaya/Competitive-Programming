#include <bits/stdc++.h>
using namespace std;

int main(){
    cin.tie(0);
    cin.sync_with_stdio(0);

    //*Really similar to the usaco problem so might use that. 
    //*We cannot simulate every single time. 
    //*Might have to go with the "when the next interesting thing happens approach". 
    //!Nah, I will look at editorial and learn. 
    //!Probably will do this problem just because I did a similar problem and wanna see how they are related. 
}