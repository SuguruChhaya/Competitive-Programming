#include <bits/stdc++.h>
using namespace std;

//!Really tedious problem so try something else. 
struct Intersection{
    long long i, j, time_
}
vector <Cow> C(50);
int main(){
    long long n;cin >> n;
    for (long long i=0;i<n;i++){
        char s;
        long long x, y;
        cin >> C[i].dir >> C[i].x >> C[i].y;

    }   
}